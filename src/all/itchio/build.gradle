ext {
    extName = "Itch.io"
    extClass = ".Itchio"
    extVersionCode = 1
    isNsfw = false
}

apply from: "$rootDir/common.gradle"

dependencies {
    compileOnly(project(":lib:hostapistubs"))
    compileOnly(libs.unifile)
}
